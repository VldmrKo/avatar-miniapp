# Секреты. Основной рубеж — они лежат вне дерева проекта,
# в %USERPROFILE%\.avatars\secrets.env, а на сервере в /opt/avatar-miniapp/.env
.env
*.env
secrets*

# Загрузки пользователей, готовые ролики, записи о задачах.
# data-tg — то же самое для локального телеграм-бота: каталоги у ботов
# разные, потому что числовые id людей в мессенджерах совпадают.
data/
data-tg/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
build/
dist/
*.egg-info/
.pytest_cache/
.ruff_cache/
